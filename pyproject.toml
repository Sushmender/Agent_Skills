[project]
name = "agent-skills"
version = "0.1.0"
description = "Multi-agent research system powered by Google ADK (Gemini) + Notion MCP"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "google-adk>=0.3.0",
    "python-dotenv>=1.0.0",
]
